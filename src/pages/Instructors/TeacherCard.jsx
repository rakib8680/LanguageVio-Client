import React from 'react';

const TeacherCard = ({ instructor }) => {

    console.log(instructor);
    const {}= instructor || {};

    return (
        <div>

        </div>
    );
};

export default TeacherCard;